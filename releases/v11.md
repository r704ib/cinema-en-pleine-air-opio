# v11 — 2026-07-26

Renommage interne : "comité" → "Oria" pour les notifications de réservation.

## Changements

- Aucun changement visible pour les visiteurs. Le contenu, l'objet et le
  destinataire des emails restent identiques (`Oria.ei@outlook.fr`).
- Renommage purement interne, pour que le code et le guide reflètent la
  réalité : c'est **Oria** qui reçoit et gère les notifications de
  réservation/annulation, pas une adresse séparée "comité".

## Technique

- `functions/email-content.js` : `COMITE_EMAIL` → `ORIA_EMAIL`,
  `buildComiteNewReservationEmail` → `buildOriaNewReservationEmail`,
  `buildComiteCancellationEmail` → `buildOriaCancellationEmail`.
- `functions/index.js` et `functions/test/email-content.test.js` mis à jour en
  conséquence.
- `GUIDE.md` : les passages décrivant "le comité reçoit un email" reformulés
  en "toi (Oria) reçois un email".
- Le site public (`public/index.html`) n'est pas concerné : les mentions
  « Comité des fêtes d'Opio » y restent inchangées, car elles désignent le
  véritable organisateur de l'événement aux yeux du public — distinct d'Oria,
  qui y est déjà listée séparément comme « Prestation technique ».
- Tests (11/11) passés avant déploiement. Fonctions redéployées.
